# Dependências só de desenvolvimento/teste. A imagem de produção instala
# apenas o requirements.txt — nada daqui vai para o container.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
pillow==12.3.0
