fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic-settings==2.15.0
motor==3.7.1
python-multipart==0.0.31
bcrypt==5.0.0
PyJWT==2.13.0
email-validator==2.3.0
# Fase 9 — o Architecture Lock precisa de pixel, não de promessa: rasterizar os
# polígonos da máscara e compor o resultado do provedor só na área permitida é
# o que impede a geração de alterar a arquitetura. Vale para o mock e para o
# provedor real (que recebe a máscara como PNG).
pillow==12.3.0
